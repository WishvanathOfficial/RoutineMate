// Kept as plain TS constants (rather than reading from SCSS) so the
// conic-gradient can be computed inline without a CSS-in-JS dependency.
export const colorBrand600 = '#4F46E5';
export const colorSlate200 = '#e2e8f0';
